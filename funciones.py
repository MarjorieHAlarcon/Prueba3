import json

def agregar_categoria(Categoria_ID:int, nombre:str):
    with open ('bliblioteca.json', mode='r') as lecturajson:
        leerJson = json.load(lecturajson)


def agregar_categoria(AutorID:int,str):
    with open ('biblioteca.json', mode='r') as lecturajson:
        leerJson = json.load(lecturajson)

def agregar_categoria(Eliminar:int, nombre:str):
    with open ('biblioteca.json', mode='r') as lecturajson:
        leerJson = json.load(lecturajson)

def agregar_categoria(Editar:int, nombre:str):
    with open ('biblioteca.json', mode='r') as lecturajson:
        leerJson = json.load(lecturajson)

def agregar_categoria(YearPublicacion:int, year:str):
    with open ('bibliotetca.json',mode='r')as lecturajson:
        leerJson = json.load(lecturajson)

def agregar_reportes():
    with open ('biblioteca.json',mode='r') as lecturajson:
        leerJson = json.load(lecturajson)

# Lo intente,  soldado caido